// Constants for managing authentication tokens
export const TOKEN_KEY = 'sg-client-token';

// Constants for managing user details
export const USER_DETAILS = 'sg-user-details';

// Exporting all constants as an object
export default {
    TOKEN_KEY,
    USER_DETAILS
};
